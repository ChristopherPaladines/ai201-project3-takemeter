# ai201-project3-takemeter
A fine-tuned text classifier built for CodePath AI201. The model classifies r/changemyview posts by argument structure — Assertion, Anecdote, or Argument — using a manually annotated dataset of 200 examples. Built with DistilBERT and evaluated against a zero-shot Groq baseline.
